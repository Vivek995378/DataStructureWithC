#include<iostream.h>
#include<conio.h>
void main()
{
   clrscr();
   int a[10],b[10],x[10],c[20],i,j;
   cout<<"\ enter 1st array:-";
   for(i=0;i<10;i++)
   {
      cin>>a[i];
   }
   cout<<"\n enter 2nd array:-";
   for(i=0;i<10;i++)
   {
      cin>>b[i];
   }
   cout<<"\n the merged array is:-";
   for(i=0;i<20;i++)
   {  if(i<=9)
      {
	c[i]=a[i];
      }
      else
	c[i]=b[i-10];
   }
   for(i=0;i<20;i++)
   {
      cout<<c[i]<<"\n";
   }
   getch();

}